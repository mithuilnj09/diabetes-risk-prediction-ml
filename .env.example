# Environment Variables Template
# Copy this file to .env for local development with custom values

# Secret key for Flask session signing
SECRET_KEY=change-this-to-a-secure-random-secret-key

# Port to bind the server to (Render/Railway/Heroku inject this automatically)
PORT=5000

# Optional: Environment mode (production / development)
FLASK_ENV=production
